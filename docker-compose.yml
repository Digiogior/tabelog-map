services:
  postgres:
    image: my-postgres18-postgis
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ./db-data:/var/lib/postgresql
    networks:
      - db

networks:
  db:
    driver: bridge
