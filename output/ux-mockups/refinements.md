# Final image refinement prompts

## 02-location-permission

Edit this UI mockup board. Preserve all text, layout, buttons, native permission dialog, phone framing, map street backgrounds, and captions exactly. Make just this UX correction: remove the blue circular current-location dot from BOTH phone maps, filling in its spot with underlying street map. Neither screen has permission to know the user's current position. Do not add any location marker in its place. Everything else unchanged.

## 03-location-recovery

Edit this UI mockup board. Preserve all text, layout, phone framing, buttons, loading/error cards, map street backgrounds, and captions exactly. Remove ALL coral red restaurant map pins (the teardrop markers with fork-and-knife symbols) from BOTH phone maps, filling their spots with underlying street map. These are loading and timeout states before a restaurant area search, so no restaurant results should be displayed. Preserve coral interface buttons and spinner; those are not map pins. Add one small thin charcoal outlined center crosshair to the center of the RIGHT phone map only, indicating an area the user can select. Do not add any blue location dots. Everything else unchanged.
